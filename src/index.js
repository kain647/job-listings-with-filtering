import React from "react";
import ReactDOM from "react-dom";
import Job from "./job-listing";

ReactDOM.render(
  <React.StrictMode>
    <Job />
  </React.StrictMode>,
  document.getElementById("root")
);
